import { CricketersFilterPipe } from './cricketers-filter.pipe';

describe('CricketersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CricketersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
